# BL-7.2 — Store-agnostic catalog + PostgreSQL back-end (MS-SQL/EF deferred)

| Field | Value |
|---|---|
| **ID** | BL-7.2 |
| **Area** | Area W1.1 — Wave 1.1: Catalog decoupling |
| **Type** | Refactor / architecture |
| **Priority** | **High** |
| **Effort** | L |
| **Status** | **Complete** (all acceptance criteria met) — Npgsql provider + conformance verified **live**; Contracts-native REST client (3b-2); **`CatalogDb` retired** from ServiceClient, the service host, and the WinUI/Testing consumers (WinUI desktop local = Npgsql store-backed via `StoreBackedCatalogService`); **provider-conformance now covers FileSystem + PostgreSQL + in-memory — ALL CONFORM**; MS-SQL/EF deferred; WinUI desktop runtime validation remains a user (online) step |

## Description
Make the catalog core **DB-independent** and supply a **cost-free relational back-end** so Wave 1.1 delivers "a service that in the back uses a database," while keeping the storage provider hidden from the end-user (they consume an instance via DI and never see the DB/EF).

- Remove the baked-in **EF Core / SQL Server** dependency from the catalog core (today `Edam.Data.CatalogModel` references `Microsoft.EntityFrameworkCore.SqlServer`; `CatalogContext` in `CatalogDb` is SQL-specific).
- Add a provider-agnostic **`ICatalogStore`** (container/item/item-data metadata) + **`IContentStore`** (binary content) — following Wave-1's `ICatalogStore` provider pattern.
- **Wave 1.1 relational back-end = PostgreSQL (Npgsql)**, free and consistent with Wave-1 BL-6.6 (`PostgresCatalogStore`), implemented behind `ICatalogStore`/`IContentStore` and registered via DI so the provider is **hidden behind instancing**.
- **MS-SQL / EF is a deferred, optional future target — NOT implemented this wave** (per ADR-0007).
- **Resource addressing is path/URI-based:** a catalog resource reference is a path-like identifier that resolves to a file, JSON, XML, or blob/binary (content-agnostic) — encoded in `IContentStore` and the item model.
- Fix layering smells: `Edam.Data.CatalogServiceClient` must **stop referencing `Edam.Data.CatalogDb`**, and **drop `Microsoft.AspNetCore.OpenApi`** from the client.

## Dependencies
- BL-7.1 (move/extract), BL-7.3 (contracts/base platform defines `ICatalogStore`/`IContentStore` + path-addressable item model).

## Acceptance criteria
- [x] Catalog core has **zero** `Microsoft.EntityFrameworkCore*` references; **no EF code in Wave 1.1**.
- [x] A PostgreSQL (`Npgsql`) `ICatalogStore`/`IContentStore` provider implements the catalog surface and is registered via DI so consumers get an instance and never reference the provider/DB (hidden back-end).
- [x] Resource access is path/URI-keyed — file/json/xml/blob/binary all resolvable through `IContentStore`/the item model.
- [x] MS-SQL/EF recorded as a deferred future option (no code).
- [x] `CatalogServiceClient` has no `CatalogDb` project reference and no `AspNetCore.OpenApi` package reference.
- [x] A future back-end (e.g. Azure blob, Wave 2) can be added without touching core.
- [x] **Provider-conformance suite** (ADR-0006 purity): the same catalog-behavior tests pass against FileSystem and PostgreSQL providers, proving back-ends swap without changing callers. (Harness written (`Edam.Data.Catalog.Conformance`); **verified offline against the in-memory reference provider and the FileSystem provider: BOTH ALL CONFORM**; **live-PostgreSQL run VERIFIED: ALL CONFORM (2026-09-15, 12/12 PASS against `edam-postgres`)**; plus an **MSTest unit-test project (`Edam.Data.Catalog.Tests`)**: FileSystem/InMemory provider conformance, FileSystem durability across instances, `IContentStore` roundtrip, and the `StoreBackedCatalogService` facade Model↔Contracts mapping.)

## Progress
- (2026-09-15) **FileSystem provider + unit tests — verified.** Added **`Edam.Data.Catalog.FileSystem`** — `FileSystemCatalogStore : ICatalogStore` and `FileSystemContentStore : IContentStore` (ADR-0006/0007: a real, durable, path-addressed file-system back-end with no DB/EF). The metadata store persists containers/items/item-data as JSON under a root dir (survives re-open); the content store maps `/docs/x` → physical files under `content/`. Registered in the solution. **Wired into the conformance runner** (`Edam.Data.Catalog.Conformance filesystem <root>` → **ALL CONFORM 12/12**, same checks as PostgreSQL + in-memory). Added **`Edam.Data.Catalog.Tests`** (MSTest): FileSystem & in-memory provider conformance, FileSystem durability across store instances, `IContentStore` roundtrip + physical-file layout, and **`StoreBackedCatalogService`** facade Model↔Contracts mapping (container/branch/data round-trip). **Full `Edam.Data.Catalog.slnx` builds 0 error.** Note: the MSTest host (`vstest.console` → testhost) cannot run inside the agent sandbox (the testhost's parent-process exit watch is `Access is denied`), so in-sandbox runtime evidence is via the conformance console runner; the MSTest suite builds 0 error and is runnable in a normal CI/user environment.
- (2026-09-15) **Step 5 COMPLETE + verified (offline builds 0 error; store-backed facade run against live Postgres — `SMOKE_OK`).** Retired the last two `Edam.Data.CatalogDb` references:
  - **WinUI desktop local back-end migrated to Npgsql** — `Edam.UI.CatalogExplorer` dropped the `CatalogDb` ProjectReference and added `Edam.Data.Catalog.PostgreSql` (+ `Contracts`). `CatalogServiceHelper.GetLocalInstance` now builds a `PostgreSqlCatalogStore` (connection from `ConnectionStrings:catalog`, falling back to the compose DSN) wrapped in the new **`StoreBackedCatalogService`** — a Model `ICatalogService` facade that adapts the provider-agnostic Contracts `ICatalogStore` onto the Model surface the desktop UI consumes (Model↔Contracts mapping for ContainerInfo/ItemInfo/ItemDataInfo/ContentTypeInfo), so the tree-builder/view-models keep working while persistence is delegated to the Npgsql store (ADR-0006/0007). The desktop catalog **stays persistent across runs without EF**. `Edam.UI.CatalogExplorer` builds **0 error**.
  - **Service host (`Edam.Data.CatalogService`) dropped `CatalogDb`** too — its only use was a commented dead block (`builder.AddSqlServerDbContext<CatalogContext>`) and the region-2 comments; the active code uses `CatalogServiceMap` + `ICatalogStore`. Removed the `CatalogDb` ProjectReference + the stray `using Edam.Data.CatalogDb;`.
  - **Runtime smoke of the facade against the live DB (`SMOKE_OK`):** a throwaway console wrapped a `PostgreSqlCatalogStore` in `StoreBackedCatalogService` and drove it through Model `ICatalogContainer`/`ICatalogItem`/`ICatalogItemData` — enlist container, `CreateBranch` (`/smoke/beta`), `GetItemByPath`, `CreateDataLeaf`+`AddItem`+`GetDataByName` ("hello-store-backed") all round-tripped. Removed the throwaway afterward.
  - **Grep confirms no `.csproj` references `Edam.Data.CatalogDb` anymore.** Full `Edam.Data.Catalog.slnx`, the service host, `Edam.UI.CatalogExplorer`, and `Edam.Test.TestBuilder` all build **0 error**.
- (2026-09-15) **Live-PostgreSQL conformance VERIFIED — ALL CONFORM (real DB).** With the `edam-postgres` container up (user `docker compose up -d --pull always`), ran `Edam.Data.Catalog.Conformance postgres "Server=localhost;Port=5432;Database=edam;User Id=edam;Password=edam"` → **12/12 PASS** (enlist/get containers, create/get branch by path, container items/root, create/get/delete data leaf, `IContentStore` write/exists/read/delete, path-addressable resources). The Npgsql provider `EnsureSchemaAsync` created the schema idempotently on a fresh volume. This closes the last deferred BL-7.2 acceptance item (provider-conformance against live PostgreSQL). Note: the sandbox can reach the DB over TCP (port 5432) but not the Docker engine pipe.
- (2026-09-15) **BL-7.4 seed toward the deferred WinUI-local step — provider resolver, verified (offline build, 0 errors).** Added `Edam.Data.Catalog.PostgreSql.CatalogProviderResolver` implementing `ICatalogProviderResolver<ICatalogStore>` / `<IContentStore>` and registered it (with `ContainerBinding`→store mapping: `PostgreSql` → the DI `ICatalogStore`/`IContentStore`, others → null) in `CatalogServices.AddCatalogServices`. This is the provider-resolution seam that will replace the WinUI desktop `GetLocalInstance` EF/`catDb` switch and lets a Container target a file system vs a database declared in DI/config (ADR-0006/0007, BL-7.4). `Edam.Data.Catalog.slnx` builds **0 error**.
- (2026-09-15) **Retirement (staged) — step 5 (consumers) partial, verified (offline build, 0 errors).** Migrated the **Testing consumers off `Edam.Data.CatalogDb`**: `Edam.Test.TestCatalogLibrary` (`AppHelper`) and `Edam.Test.TestBuilder` dropped their `CatalogDb` ProjectReferences and now use the pure-Model `ModelCatalogService.Default` (the EF `CatalogBuilderServiceInstance` was dead — nothing read it); removed the stray `Microsoft.EntityFrameworkCore` using in `TestFileSystemClient.cs`. `Edam.Test.TestBuilder` builds **0 error** without `CatalogDb`. **WinUI desktop local back-end deferred to BL-7.4:** `Edam.UI.CatalogExplorer.CatalogServiceHelper.GetLocalInstance` on the Windows desktop uses the EF-backed `catDb.CatalogInstance`; replacing it with the in-memory `ModelCatalogService` would regress desktop persistence, so its migration is deferred to the BL-7.4 Npgsql/DI wiring + a user online build. Removed the unused plain `using Edam.Data.CatalogDb;` from `CatalogServiceHelper.cs`/`CatalogViewModel.cs` (kept the `catDb` alias the desktop local path needs).
- (2026-09-15) **Retirement (staged) — step 4 done + verified (offline build, 0 errors).** Dropped the **`Edam.Data.CatalogDb` project reference from `Edam.Data.CatalogServiceClient`** — meeting the acceptance criterion *"CatalogServiceClient has no `CatalogDb` project reference."* The only real CatalogDb type use was `CatalogFileSystemClient`'s `CatalogServiceInstance.DefaultInstance`; added a **pure-Model in-memory catalog service** (`Edam.Data.CatalogModel.ModelCatalogService` + `ModelCatalogContainer`/`ModelCatalogItem`/`ModelCatalogItemData`, EF-free) and pointed the file-system client at `ModelCatalogService.Default`. Removed the (unused) `using Edam.Data.CatalogDb;` / EF usings from all ServiceClient files, incl. `WebApp/CatalogSystem.cs`. **Full `Edam.Data.Catalog.slnx` builds `0 error`; WinUI `Edam.UI.CatalogExplorer` library also `0 error` (no regression).** Remaining: **step 5** — migrate the WinUI/Testing consumers (`Edam.UI.CatalogExplorer`, `Edam.Test.TestBuilder`, `Edam.Test.TestCatalogLibrary`) onto the Contracts platform (`CatalogHttpClient` / `ModelCatalogService` / `ICatalogStore` seams) and drop their direct `CatalogDb` references.
- (2026-09-15) **Retirement (staged) — sub-step 3b-2 done + verified (offline build, 0 errors).** Added a **Contracts-native REST catalog client** group to `Edam.Data.CatalogServiceClient` — `CatalogHttpClient` (implements `Edam.Data.Catalog.Contracts.ICatalogClient`) plus `CatalogHttpContainer` / `CatalogHttpItem` / `CatalogHttpItemData` (the `Contracts.ICatalogContainer` / `ICatalogItem` / `ICatalogItemData` surfaces). These deserialize the service's **`Contracts` value records** (ContainerInfo/ItemInfo/ItemDataInfo/ContentTypeInfo) directly — i.e. the single canonical wire/domain contract of ADR-0008 — with **no `Edam.Data.CatalogModel` EF entity set and no `Edam.Data.CatalogDb` dependency** for the HTTP path. HTTP routes mirror `CatalogServiceMap`. **Full `Edam.Data.Catalog.slnx` builds `0 error` (offline, `GeneratePackageOnBuild=false` via VS MSBuild).** These are the transition clients for step 5 (WinUI/Testing); the Model-based `CatalogClient`/`ClientCatalog*`/`CatalogFileSystemClient` path and `CatalogDb` are retired by sub-steps 4/5.
- (2026-09-15) **Retirement (staged) — sub-step 1 done:** relocated `CatalogBaseClient` **out of `Edam.Data.CatalogDb` into `Edam.Data.CatalogModel`** (it is a pure client base — WebApiClient + catalog model contracts, **no EF/DB dependency**; it sat in the DB project by accident, beside the `ICatalogBaseClient` it implements). All 10 referencing files already `using Edam.Data.CatalogModel;`, so the move is un-ambiguous. EF-based `CatalogServiceInstance`/`CatalogContext` etc. (which extended it) remain in `CatalogDb` for the next sub-steps. Note: Model still carries EF (`Microsoft.EntityFrameworkCore.SqlServer`) until sub-step 2, so sub-step-1 landings are not yet offline-buildable.
- (2026-09-15) **Retirement (staged) — sub-step 2 done + verified offline:** stripped all EF from the catalog **core** `Edam.Data.CatalogModel` — removed the `Microsoft.EntityFrameworkCore.SqlServer` package + the `[Index]` attribute + EF usings in `ContainerInfo.cs`/`ContainerType.cs` (remaining `[Table]`/`[Key]`/`[MaxLength]`/`[ForeignKey]` are EF-free data annotations). `Edam.Data.CatalogModel` now **builds offline, 0-error**. `CatalogDb` got a **temporary EF bridge** (its own EFSqlServer 9.0.2 reference, documented) so the graph still compiles online until it is retired in sub-steps 3-4.
- (2026-09-15) **Retirement (staged) — sub-step 3a done:** wired the Npgsql provider into the running catalog service via DI — `builder.Services.AddCatalogServices(builder.Configuration)` in `Edam.Data.CatalogService\Program.cs`, `appsettings.json` gains `ConnectionStrings:catalog` (Postgres DSN; the `catalogDb` MS-SQL string stays as the deferred target). No behavioral change yet (endpoints still back onto `CatalogDb`).
- **Wire-contract gate found for the endpoint rewire (sub-steps 3b/4):** the catalog endpoints return `Edam.Data.CatalogModel`'s **rich EF entities** (`ContainerInfo`/`ItemInfo` with many props), while the Npgsql `ICatalogStore` returns `Edam.Data.Catalog.Contracts`' **lean value records** (`ContainerInfo(Guid Id, string ContainerId, string Description, ContainerType ContainerType, string ContainerUri, string ContentType)`, etc.). Switching endpoints onto the store **changes the JSON wire contract** that the HTTP clients (`CatalogClient`, WinUI) deserialize into — so it is a breaking change, not mechanical, and must be landed with compile+runtime verification (user online build + live Postgres) plus an explicit reconciliation of the two `ContainerType` enums (Model vs Contracts). **Not done blind.** -> **DECISION (ADR-0008, 2026-09-15): migrate the service clients/UI onto the `Contracts` value-record shapes** as the single canonical wire/domain contract. Service endpoints will emit Contracts records via the Npgsql `ICatalogStore`; `CatalogClient`/`ClientCatalog*`/WinUI deserialize/bind Contracts records; the `CatalogModel` EF entity set + `CatalogDb` are retired from the active path. The agent **can compile-verify** each sub-step (it has internet/NuGet); runtime verification uses the live Postgres the user starts. Remaining sub-steps: 3b (endpoints emit Contracts records), 4 (drop `CatalogDb`), 5 (WinUI/Testing).
- (2026-09-15) **Retirement (staged) — sub-step 3b-1 done + verified (online build, exit 0):** rewrote `CatalogServiceMap` to back every catalog endpoint onto the Npgsql `ICatalogStore` and **emit `Contracts` value records** (routes unchanged; `Program.cs` injects the store; `CatalogServiceClient` gained a `Contracts` ProjectReference). The service no longer serves catalog data from the EF `CatalogDb`. **Step 3b-2 pending:** move `CatalogBaseClient`/`CatalogClient`/`ClientCatalog*`/`CatalogFileSystemClient` deserialization onto the `Contracts` records.
- (2026-09-15) **BL-7.2 acceptance win — `CatalogServiceClient` no `Microsoft.AspNetCore.OpenApi` (verified online, exit 0):** the package was unused (no OpenAPI code) but supplied `WebApplication` transitively to the hosted map. Replaced with `FrameworkReference Include="Microsoft.AspNetCore.App"`; package dropped. `CatalogServiceClient` still references `CatalogDb` via `CatalogFileSystemClient`/`CatalogInstance` — removal pending step 4 (needs the 3b-2 client rewrite).
- (2026-09-15) Started. Added the catalog **`ICatalogStore`** metadata seam to `src\Edam.Data.Catalog\Edam.Data.Catalog.Contracts\` (aggregates `ICatalogContainer`/`ICatalogItem`/`ICatalogItemData`, plus `DescribeStore()`), completing the **`ICatalogStore`/`IContentStore`** store+content seam pair BL-7.2 names. **Verified offline: Contracts builds net10, 0-error.**
- (2026-09-15) **PostgreSQL environment provisioned as a container.** Added root `compose.yaml` (service `postgres`, image `postgres:17-alpine`, container `edam-postgres`, persistent volume `edam_pgdata`, healthcheck, `localhost:5432`). **Npgsql connection string: `Server=localhost;Port=5432;Database=edam;User Id=edam;Password=edam`.** Container must be started by a human: `docker compose up -d --pull always` — the agent sandbox cannot reach the Docker engine pipe (open //./pipe/dockerDesktopLinuxEngine = `Access is denied`, same boundary as `git push`).
- (2026-09-15) **Npgsql provider + DI + provider-conformance suite — verified offline.** New `Edam.Data.Catalog.PostgreSql` provider (`PostgreSqlCatalogStore : ICatalogStore`, `PostgreSqlContentStore : IContentStore`, idempotent schema, `AddCatalogServices()` DI) references **only `Contracts` + Npgsql** (no Model/EF). New `Edam.Data.Catalog.Conformance` harness runs the **same provider-agnostic scenario** (ADR-0006) against any `ICatalogStore`/`IContentStore` — **built & run offline against the in-memory reference provider: ALL CONFORM (exit 0)**. Run against live Postgres with `Edam.Data.Catalog.Conformance postgres "<connection>"` (user shell).
- **Gate found (recorded):** the whole catalog graph references `Edam.Data.CatalogModel`, which still carries the **uncached `Microsoft.EntityFrameworkCore.SqlServer`** package — so **no EF-coupled project builds offline until EF is removed**. Removing EF from Model requires rewriting `CatalogDb`'s EF data layer onto **Npgsql** (runtime-verified against a live PostgreSQL = **user shell**). Remaining BL-7.2 chunks: (1) `CatalogDb` → Npgsql + Model EF removal (now that the Npgsql path is proven, this is the safe next step); (2) `CatalogServiceClient` decouple (drop `CatalogDb` + `Microsoft.AspNetCore.OpenApi`); (3) DI wiring into a consumer host; (4) run the conformance suite against live Postgres.

## Why
Delivers the Wave-1.1 goal of a **service backed by a database at no cost** (PostgreSQL), consistent with Wave-1 persistence; keeps Microsoft SQL/EF as an optional future target; honors the hidden-provider/instancing mandate (ADR-0007, ADR-0006).
